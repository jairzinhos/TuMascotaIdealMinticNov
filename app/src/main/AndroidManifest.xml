<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grupo.ejemplo1">

    <application
        android:name=".Aplicacion"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ejemplo1.NoActionBar">
        <activity
            android:name=".presentacion.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentacion.AcercaDeActivity"
            android:label="Acerca de"
            android:theme="@style/Theme.Ejemplo1.NoActionBar"
        ></activity>
        <activity android:name=".presentacion.VistaMascotaActivity"
            android:label="Vista Mascota"
            android:screenOrientation="portrait">
        </activity>



    </application>

</manifest>